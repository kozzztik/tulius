.player-avatar
  .player-avatar-image
    - if player.avatar
        %img{src: '{{ player.avatar.url }}', id: 'avatar{{ player.id }}'}
    - else
        %img{src: '{{ MEDIA_URL }}blank_avatar.jpg', id: 'avatar{{ player.id }}'}
